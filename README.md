# Oddbirds Stays Website

Static HTML/CSS/JS prototype for the Oddbirds Stays Website. Uses only HTML, CSS and vanilla JS — no build step required.

To view locally:

1. Open `D:/Oddbirds/index.html` in your browser.

Notes:

- Default preview image for the "Our Spaces" card is `Images/balcony.png`.
- Room A and Room B have crossfading slideshows using local images in the `Images/` folder.
- Mobile navigation and a small interactive demo are implemented in `script.js`.

